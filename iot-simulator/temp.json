{
  "name": "iot-simulator-clean",
  "version": "3.0.0",
  "description": "Clean IoT simulator with single-file storage - no timestamp errors!",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist/ output/ node_modules/.cache"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.6.0"
  }
}
    "delhi": "ts-node src/delhiDemo.ts",
    "delhi:build": "npm run build && node dist/delhiDemo.js"
  }
}
